<section id="contact" class="page-content contact-section">
    <div class="contact-box">
        <h2>Let's Engineer Your Traffic Growth</h2>
        <p>Ready to scale your organic visibility using data automation and advanced technical SEO? Let's discuss your project goals on Upwork.</p>
        
        <!-- Замени '#' на реальную ссылку на свой профиль Upwork -->
        <a href="#" target="_blank" rel="noopener noreferrer" class="cta-button">
            Hire Me on Upwork 
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
    </div>
</section>

<style>
    .contact-section {
        padding: var(--section-gap) 0 var(--section-gap);
        display: flex;
        justify-content: center;
    }
    .contact-box {
        background: radial-gradient(100% 100% at 50% 0%, rgba(0, 240, 255, 0.04) 0%, rgba(0, 0, 0, 0) 100%), rgba(255, 255, 255, 0.01);
        border: 1px solid rgba(0, 240, 255, 0.1);
        padding: 4rem 2rem;
        border-radius: 8px;
        text-align: center;
        max-width: 700px;
        width: 100%;
        box-shadow: 0 20px 40px rgba(0,0,0,0.2);
    }
    .contact-box h2 {
        color: #fff;
        font-size: clamp(1.5rem, 3vw, 2.25rem);
        margin: 0 0 1rem;
        font-weight: 800;
    }
    .contact-box p {
        color: var(--mako-text);
        font-size: 1.05rem;
        line-height: 1.6;
        margin: 0 0 2.5rem;
    }
    .cta-button {
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        background: var(--mako-cyan);
        color: #000;
        text-decoration: none;
        padding: 1rem 2.25rem;
        font-weight: 700;
        font-size: 0.95rem;
        border-radius: 4px;
        text-transform: uppercase;
        letter-spacing: 1px;
        transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
        box-shadow: 0 0 20px rgba(0, 240, 255, 0.2);
    }
    .cta-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 0 30px rgba(0, 240, 255, 0.4);
        background: #fff;
    }
    .cta-button svg {
        transition: transform 0.3s ease;
    }
    .cta-button:hover svg {
        transform: translateX(4px);
    }
</style>